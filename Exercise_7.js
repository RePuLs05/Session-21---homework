const currentYear = new Date().getFullYear();
const yearsJumped = -904; 

const newYear = currentYear + yearsJumped;
if(newYear==1121){
  console.log(`fight with DAVIT AGMASHENEBELI on didgori: ${newYear} year `);
  return 0;
}
console.log(`The new year after the time jump is: ${newYear} `);
